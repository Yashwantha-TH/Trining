//You are given an array representing a group of players’ jersey numbers sitting in a circle. 
//All players have the same jersey number except for one imposter who has a different jersey number.
//Write a program that finds the imposter’s jersey number.

package hhh;

public class peo11 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		int arr[]= {2,3,3,3,3,3,3};
		int imposter=0;
		 for(int i=1;i<arr.length;i++) {
			 if(arr[i]!=arr[i+1]) {
			 imposter=arr[i+1];
			 break;
		 } else  if(arr[1]!=arr[0]) {
			 
			 
			
				 imposter=arr[0];
				 break;
				 
			 }
			 
		 }
		 System.out.println(imposter);

	}


}
